'use strict';

const { CommandInteraction, MessageEmbed } = require("discord.js");

/**
 * @param {CommandInteraction} inter 
 */
async function ranLog(inter, addition) {
    const emb = new MessageEmbed();
}